{% macro wishlistTemplate(sessionCustomerId, hasWishlist, productId) %}
    {% if sessionCustomerId and hasWishlist %}
        <i class="fas fa-heart color-blue btn-add-wishlist wishlist-{{ productId }}" data-product-id="{{ productId }}" title="{% trans %}Remove From Wishlist{% endtrans %}?" data-title-on="{% trans %}Remove from Wishlist{% endtrans %}?" data-title-off="{% trans %}Add to Wishlist{% endtrans %}?"></i>
    {% elseif sessionCustomerId %}
        <i class="far fa-heart color-blue btn-add-wishlist wishlist-{{ productId }}" data-product-id="{{ productId }}" title="{% trans %}Add to Wishlist{% endtrans %}?" data-title-on="{% trans %}Remove from Wishlist{% endtrans %}?" data-title-off="{% trans %}Add to Wishlist{% endtrans %}?"></i>
    {% else %}
        <i class="far fa-heart color-blue btn-login" title="{% trans %}Login than add to Wishlist{% endtrans %}"></i>
    {% endif %}
{% endmacro %}